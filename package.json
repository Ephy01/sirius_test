{
  "name": "sirius-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "UI prototype for the Sirius intellectual contest platform",
  "scripts": {
    "dev": "vite",
    "dev:api": "python3 -m uvicorn app.main:app --app-dir backend --reload --host 127.0.0.1 --port 8001",
    "build": "tsc -b && vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.0.0",
    "typescript": "^6.0.0",
    "vite": "^8.1.0"
  }
}
